/**
 * paywalls receipts — View recent transaction receipts.
 *
 * Calls GET /api/wallet/:accountPublicId/receipts with API key auth.
 */
export async function receipts(_args: string[]): Promise<void> {
  console.log('paywalls receipts — not yet implemented');
  console.log('');
  console.log('This will display your recent transaction receipts.');
}

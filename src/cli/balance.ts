/**
 * paywalls balance — Check wallet balance.
 *
 * Calls GET /api/wallet/:accountPublicId/balance with API key auth.
 */
export async function balance(_args: string[]): Promise<void> {
  console.log('paywalls balance — not yet implemented');
  console.log('');
  console.log('This will display your current wallet balance.');
}
